function add(a:number, b:number):number{    //a,b is typed to take only "number" type input.
    let sum:number = a+b;                   //function add is also typed to return a "number" type value.
    
    return sum;
    //return "Hello" ❌

}
add(2,3);